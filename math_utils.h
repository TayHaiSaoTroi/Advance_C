#ifndef MATH_UTILS_H              ifndef dùng để kiểm tra 1 macro đã dược định nghĩa hay chưa, nếu chưa định nghĩa thì nó sẽ chạy tiếp tục
#define MATH_UTILS_H                   các tác vụ cho đến endif    
int add(int a, int b);

#endif // Kết thúc của #ifndef
